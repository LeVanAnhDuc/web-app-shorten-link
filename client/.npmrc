# Mirrors the old `client/.yarnrc` "ignore-engines true": package.json declares
# engines.node "^22.18.0 || >=24.12.0", but dev machines still run older Node 22.
# Drop this once every dev machine is on the declared range.
engine-strict=false
